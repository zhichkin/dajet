CREATE TYPE ENTITY
(
  SELF REF Ссылка uuid    NOT NULL PRIMARY KEY,
  ВерсияДанных    version NOT NULL,
  ПометкаУдаления boolean NOT NULL
);

CREATE TYPE ТабличнаяЧасть
(
  KeyField    binary(4)    NOT NULL PRIMARY KEY,
  НомерСтроки numeric(5,0) NOT NULL
);