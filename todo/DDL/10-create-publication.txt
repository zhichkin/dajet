CREATE NAMESPACE ПланОбмена;

CREATE TYPE ПланОбмена AS ENTITY
(
  Предопределённый   uuid,
  Код                string[8],
  Наименование       string(30),
  НомерПринятого     numeric(10),
  НомерОтправленного numeric(10)
);

CREATE TYPE ПланОбмена.ЭкспортДанныхПоставщикам AS ПланОбмена
(
  -- ???
);

CREATE TABLE ЭкспортДанныхПоставщикам OF ПланОбмена.ЭкспортДанныхПоставщикам;